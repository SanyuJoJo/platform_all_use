# 后端 API 基地址（必填）
VITE_API_BASE_URL=http://localhost:8080
# 主应用域名（用于子应用资源加载）
VITE_MAIN_APP_DOMAIN=http://localhost:3000
# 子应用公共路径前缀（用于部署）
VITE_SUB_APP_PREFIX=/subapp
# 是否开启 mock（开发阶段使用）
VITE_USE_MOCK=false
